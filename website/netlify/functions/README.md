# Generated at build time

build.sh writes corpus.md and the bundled chat.mjs function here.
Both are git-ignored; this file keeps the directory present in fresh clones
(netlify.toml declares it as the functions directory before the build runs).
